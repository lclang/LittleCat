/***
    Stream module
    From: 0.1.1
    Minimal version: 0.1.2
**/

method readInput(input): string {
    lines = ""
    while input.hasNextLine(): lines += input.readLine() + "\r\n"

    return lines
}

method print(value): void {
    std.output.print(value)
}

method println(value): void {
    std.output.println(value)
}