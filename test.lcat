global TEST = "error!";

method main() {
    data = [time(), "a", "b", "c"];

    //Обычный комментарий
    /*Многострочный комментарий*/
    [time] = data;

    println(asString(data));

    print("Current time: ");
    println([time][0]);
    //println(typeTest);
    print(data[]);
    printError(TEST);
    exit(1);

    println("program");
    println(readLine());
}

main();