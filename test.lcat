global TEST = "error!";

method main() {
    print("Current time: ");
    println(time());

    super = "My super ";
    print(super);
    printError(TEST);
    exit(1);
    println("program");
    println(readLine());
}

main();