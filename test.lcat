global TEST = "error!";

method main() {
    print("Current time: ");
    println(time());
    println(*TEST);
    print(super = "My super ");
    printError(TEST);
    exit(1);
    println("program");
    println(readLine());
}

main();